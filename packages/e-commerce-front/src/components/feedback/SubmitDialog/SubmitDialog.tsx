import type { SubmitDialogProps } from "@/components/feedback/SubmitDialog/types";

const SubmitDialog = ({}: SubmitDialogProps) => {
  return <div>SubmitDialog</div>;
};

export default SubmitDialog;
