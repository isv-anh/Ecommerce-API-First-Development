name: "[CI] E-Commerce Search Build and Test"

on:
  pull_request:
    types: [assigned]
    paths:
      - "packages/e-commerce-search/**"
      - "packages/proto/**"
      - ".github/workflows/ci-e-commerce-search.yml"
      - "!packages/e-commerce-search/.vscode/**"

jobs:
  resolve-runner:
    runs-on: ubuntu-24.04
    outputs:
      runner: ${{ steps.set.outputs.runner }}
    steps:
      - name: Set runner
        id: set
        run: |
          if echo '${{ toJSON(github.event.pull_request.labels.*.name) }}' | grep -q 'self-hosted'; then
            echo 'runner=["self-hosted", "${{ github.event.sender.login }}"]' >> $GITHUB_OUTPUT
          else
            echo 'runner=["ubuntu-24.04"]' >> $GITHUB_OUTPUT
          fi

  ci-check:
    needs: resolve-runner
    runs-on: ${{ fromJSON(needs.resolve-runner.outputs.runner) }}
    env:
      NODE_VERSION: "22.17.0"
    defaults:
      run:
        working-directory: ./packages/e-commerce-search
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install protoc
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install Node.js ${{env.NODE_VERSION}}
        uses: actions/setup-node@v6
        with:
          node-version: ${{env.NODE_VERSION}}
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Protobuf
        run: pnpm run proto:generate

      - name: Build
        run: pnpm run build
